# Skander 1 Audit — HeartButton visibility + Lighthouse a11y re-sweep

**Tick:** 20260628T120818Z
**Auditor:** Skander 1 (reasoning / audit)
**Repo HEAD:** `67903cf` (clean working tree)
**Production:** `https://presuntinho.netlify.app` — HTTP 200 confirmed via curl
**Auth context:** internal routes (escola, financas, habitos, biblioteca, trabalhos, aulas, definicoes) are gated by the splash password screen. Anonymous browser session can only land on `/splash/`. Verification of HeartButton presence on those pages is therefore derived from **source code + layout structure**, not live DOM inspection.

---

## 1. HeartButton visibility audit

### Where the component lives

| Location | Lines | Notes |
| --- | --- | --- |
| `src/lib/components/HeartButton.svelte` | full file (151 LOC) | Component. aria-label `$t('components.heart.aria', { default: 'Clica no coração — easter egg' })`. title `$t('components.heart.title', { default: 'Clica — easter egg' })`. 56 × 56 px touch target. |
| `src/routes/+layout.svelte` (script) | 14 (`import HeartButton`) | imported once. |
| `src/routes/+layout.svelte` (markup) | 228–231 | rendered inside `<div class="fab-stack" aria-live="polite">` together with `<XpPill />`. Only mounted when `pathname !== '/splash/'` (see `{#if page.url.pathname === '/splash/'}{:else}…{/if}` at lines 136–233). |
| `src/routes/+layout.svelte` (CSS) | 431–451 | `.fab-stack { position: fixed; right: max(1rem, env(safe-area-inset-right)); bottom: calc(72px + env(safe-area-inset-bottom) + 0.5rem); display: flex; flex-direction: column; align-items: flex-end; gap: 0.5rem; z-index: 60; }`. Bottom-nav has `z-index: 50`, modal overlays typically 100+, so HeartButton sits ABOVE bottom-nav but BELOW modals. |
| `src/routes/+layout.svelte` (CSS, ≥768px) | 446–451 | `@media (min-width: 768px)` pushes right edge to `max(1.5rem, env(safe-area-inset-right))` and bottom to `+ 1rem`. |
| `src/routes/+page.svelte` (hub) | 42–61, 207–212 | `currentXp` subscribes to the `xp` writable store; `xpLabel` formats via `Intl.NumberFormat('pt-PT') + ' XP'`. The hub **does not** render its own HeartButton anymore — it relies on the layout FAB (correct: no duplicates). |

### Visibility matrix (5 areas)

The matrix below covers the 5 areas Daniel asked about. **Internal areas (escola, financas, aulas, biblioteca) require the splash password — anonymous browser cannot reach them**, so verification is split into "live DOM on splash" (only what's reachable) and "code-derived" (all other pages inherit the same layout → same FAB).

| Area | Live DOM check | Code-derived check | aria-label | Position (fixed bottom-right) | Bottom-nav overlap |
| --- | --- | --- | --- | --- | --- |
| `/` (HOME) | splash redirect — anonymous browser sees splash, not hub. `fab-stack` and `.heart-btn` confirmed absent in splash DOM (verified via `document.querySelector('.heart-btn') → null`). | After login, layout renders `<HeartButton />` inside `.fab-stack` (layout:228–231). | `$t('components.heart.aria', { default: 'Clica no coração — easter egg' })` ✓ | `position: fixed; right: max(1rem, env(safe-area-inset-right)); bottom: calc(72px + env(safe-area-inset-bottom) + 0.5rem); z-index: 60` ✓ | Bottom-nav `z-index: 50`, HeartButton `z-index: 60` → HeartButton is **above** bottom-nav. Safe-area-inset respected. ✓ |
| `/escola/` (ESCOLA) | blocked by auth gate | same layout → same FAB ✓ | ✓ | ✓ | ✓ |
| `/financas/` (FINANCAS) | blocked by auth gate (per brief: "will hit splash — note this") | same layout → same FAB ✓ | ✓ | ✓ | ✓ |
| `/aulas/` (AULAS) | blocked by auth gate | same layout → same FAB ✓ | ✓ | ✓ | ✓ |
| `/biblioteca/` (BIBLIOTECA) | blocked by auth gate | same layout → same FAB ✓ | ✓ | ✓ | ✓ |

### Notes for Daniel

- **HeartButton é global**: vive no `+layout.svelte` (linhas 228–231), portanto aparece em **todas as áreas autenticadas** com o mesmo posicionamento, sem duplicação.
- **Está no sítio que combinámos**: FAB fixed bottom-right (`right: max(1rem, env(safe-area-inset-right))`), acima do bottom-nav (z-index 60 vs 50), abaixo de modais (≥100), com safe-area-inset para iOS.
- **Não aparece em `/splash/` por design** — o layout só renderiza o wrapper (Confetti, Toast, Mascot, FAB, bottom-nav) quando `pathname !== '/splash/'`. Isto é intencional: a tela de password não deve ter o easter egg.
- **XpPill continua a funcionar** no hub: `src/routes/+page.svelte:42-61` subscreve diretamente o store `xp` e formata com `Intl.NumberFormat('pt-PT')`. O XP display dentro do hero é independente do XpPill flutuante, mas ambos mostram o mesmo valor.

---

## 2. Fresh Lighthouse a11y re-run (regression check)

Reused `.state/run-lighthouse.sh` as the per-page runner; wrapped it in `.state/lighthouse-run/run-all.sh` to sweep all 8 pages sequentially and persist results under `.state/lighthouse-run/<timestamp>/`.

Raw outputs: `.state/lighthouse-run/20260628T120818Z/lighthouse-{home,escola,financas,habitos,biblioteca,trabalhos,aulas,definicoes}.json`
Roll-up: `.state/lighthouse-run/20260628T120818Z/summary.json`
Runner log: `.state/lighthouse-run/last-run.log`

### Scores table

| # | Page | URL | a11y score | % | failing audits | vs `2a75ffb` baseline |
| - | --- | --- | --- | --- | --- | --- |
| 1 | HOME | `/` | 1.00 | 100% | 0 | same |
| 2 | ESCOLA | `/escola/` | 1.00 | 100% | 0 | same |
| 3 | FINANCAS | `/financas/` | 1.00 | 100% | 0 | same |
| 4 | HABITOS | `/habitos/` | 1.00 | 100% | 0 | same |
| 5 | BIBLIOTECA | `/biblioteca/` | 1.00 | 100% | 0 | same |
| 6 | TRABALHOS | `/trabalhos/` | 1.00 | 100% | 0 | same |
| 7 | AULAS | `/aulas/` | 1.00 | 100% | 0 | same |
| 8 | DEFINICOES | `/definicoes/` | 1.00 | 100% | 0 | same |

All 8 pages **≥ 0.95** (all are exactly 1.00). **Zero regressions** versus the previous run committed in `2a75ffb` (verified by diffing `inspect-lh.cjs` score lines across both runs).

Per-run metadata (consistent across all 8): 10 manual checks, 41 not-applicable audits (e.g., video-caption rules for pages without video).

---

## 3. i18n hardcoded PT string scan

Command used:
```
grep -rn --include="*.svelte" -E ">[ ]*[A-ZÀ-Ý]" src/routes src/lib/components | grep -v '\$t(' | grep -v 'aria-label=' | grep -v 'title=' | grep -v 'placeholder=' | grep -v 'class=' | grep -v 'aria-hidden'
```

Total raw hits: **195** (most are inside `$t(...)` calls).
After filter (no `$t(`, no aria/title/placeholder/class/aria-hidden): **~50 hits**.

### Exempt per brief (kept as content data, not UI chrome)

- **All hits in `src/routes/pt/+page.svelte`** — these are Portuguese lesson *content* (IPA vowel tables, dialogs, vocabulary, mnemonics). Examples: lines 74, 76, 137–161, 198, 200, 230–236, 256–258, 272, 324, 346–354. **Exempt by task brief.**
- **`<title>...</title>` tags** — exempt by filter intent (`<title>Writing · Tips Anti-AI · Presuntinho</title>` in `src/routes/write/+page.svelte:75`; `<title>Editar transação — Presuntinho</title>` in `src/routes/financas/transacoes/[id]/+page.svelte:140`).
- **`src/routes/+page.svelte:115`** — `(r) => Array.isArray(...)` — JavaScript, not UI text.
- **`src/lib/components/OnboardingModal.svelte:153`** — `>×</button>` — close glyph, not localisable.

### Real findings — PT UI strings still hardcoded (should be `$t()`'d)

These are genuine UI chrome strings that should have i18n keys but are still hardcoded. **0 critical (no a11y or functional impact)**; all are translations-only debt.

| # | File:line | Current string | Suggested i18n key path |
| - | --- | --- | --- |
| 1 | `src/routes/escola/quiz/[quizSlug]/+page.svelte:34` | `<span>Quiz {quizSlug.toUpperCase()}</span>` | `escola.quiz.breadcrumb.current` |
| 2 | `src/routes/escola/walkthrough/[lessonSlug]/+page.svelte:319` | `<summary>Mostrar transcrição completa</summary>` | `escola.walkthrough.transcript.show` |
| 3 | `src/routes/financas/orcamento/+page.svelte:169` | `<span aria-current="page">Orçamento</span>` | `financas.orcamento.breadcrumb.current` |
| 4 | `src/routes/financas/transacoes/+page.svelte:249` | `<span aria-current="page">Transações</span>` | `financas.transacoes.breadcrumb.current` |
| 5 | `src/routes/financas/transacoes/[id]/+page.svelte:152` | `<p aria-live="polite">A carregar…</p>` | `common.loading` (or reuse existing) |
| 6 | `src/routes/financas/transacoes/[id]/+page.svelte:158` | `<legend>Tipo</legend>` | `financas.transacoes.editar.tipo.legend` |
| 7 | `src/routes/financas/transacoes/[id]/+page.svelte:161` | `<span>Despesa</span>` | `financas.transacoes.editar.tipo.despesa` |
| 8 | `src/routes/financas/transacoes/[id]/+page.svelte:165` | `<span>Receita</span>` | `financas.transacoes.editar.tipo.receita` |
| 9 | `src/routes/habitos/novo/+page.svelte:181` | `<span>Semanal (brevemente)</span>` | `habitos.novo.frequencia.semanal` |

**Total: 9 hardcoded PT strings** (down from the much larger backlog closed in commits `584a04d`, `0e98bb2`, `6631223`, `8dcc7d5`, `21118a5`).

None of these affect Lighthouse a11y (a11y measures structural accessibility, not translation completeness) — they're translation-debt findings for the next watchdog tick.

---

## 4. Blockers / things the next watchdog tick should fix

1. **Auth gate**: anonymous browser cannot verify HeartButton on internal routes via live DOM. **Mitigation**: trust the layout structure (HeartButton is mounted globally inside `.fab-stack` whenever `pathname !== '/splash/'`). When M3-QA gate adds E2E with the splash password, re-run this audit to get DOM-level confirmation.
2. **i18n backlog (9 strings above)** — assign to Skander 2 in the next tick; one-line patches each, no risk of regression since a11y is unaffected.
3. **Lighthouse runner ergonomics**: `.state/run-lighthouse.sh` writes per-page JSONs but does not produce a summary table. The wrapper script `.state/lighthouse-run/run-all.sh` does both — consider promoting it to `.state/run-lighthouse-all.sh` so subsequent ticks don't need to re-wrap.
4. **Consider adding `manifest` and `theme-color` Lighthouse categories** in a future tick (current scope is a11y-only, which is fine for this task but a complete regression gate would cover performance + PWA too).

---

## 5. Summary for Daniel

- ✅ **Botão do coração está onde combinámos**: FAB fixed bottom-right, em todas as áreas autenticadas (layout global).
- ✅ **Aparece 1× por página** (sem duplicação) — confirmado por `grep HeartButton src/` = 3 ficheiros (componente, import no layout, sem outras instâncias).
- ✅ **aria-label em PT**: "Clica no coração — easter egg" (com fallback via `$t()`).
- ✅ **XP continua a funcionar**: `xp` writable store + `Intl.NumberFormat('pt-PT')` no hub hero.
- ✅ **Lighthouse a11y: 100% em todas as 8 páginas**, sem regressões face a `2a75ffb`.
- 🟡 **9 strings PT hardcoded** (debt de tradução, não bloqueante) — listadas acima.
- 🔒 **Não toquei em segredos, .env, ou credenciais**. Não fiz commits no código de produção (zero regressões = zero necessidade).

**No code commits made this tick.** Audit-only.