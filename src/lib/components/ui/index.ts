/**
 * Design-system primitives (Presuntinho V8).
 *
 * Import from `$lib/components/ui`:
 *
 *   import { Card, Button, PageHeader, StatTile, SectionTitle } from '$lib/components/ui';
 *
 * All primitives are token-driven (see src/app.css) so every theme —
 * light, dark, barca, tunisia, … — restyles them automatically.
 */
export { default as Card } from './Card.svelte';
export { default as Button } from './Button.svelte';
export { default as PageHeader } from './PageHeader.svelte';
export { default as StatTile } from './StatTile.svelte';
export { default as SectionTitle } from './SectionTitle.svelte';
